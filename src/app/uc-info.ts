export interface UcInfo {
    className: string;	//The name of the class.
    totalCount :number; //of words the class is trained on.
    uniqueFeatures :number; //of unique words the class is trained o
}
